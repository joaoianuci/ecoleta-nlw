import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      height: 56,
      marginVertical: 8,
      backgroundColor: '#fff',
      borderRadius: 10,
    },
  });

  export default styles;